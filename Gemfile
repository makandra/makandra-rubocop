source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in makandra-rubocop.gemspec
gemspec

gem 'bundler', '~> 2.0'
gem 'rake', '~> 12.3'
